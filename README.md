# Notas:

Primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Is the base of the Multiplication table     [number] [required]
  -h, --hasta    Final number to resolve                  [number] [default: 10]
  -l, --listar   Show the table in console            [boolean] [default: false]

```